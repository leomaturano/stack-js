{% extends "_layouts/default.njk" %} {% block body %}
<div class="content">
  {% include "_partials/flash.njk" %}
  <strong>Olá, {{ user.name }}</strong>
  <p>Inicie seu agendamento escolhendo um profissional abaixo:</p>
  <ul class="providers">
    {% for provider in providers %}
    <li class="provider">
      <div>
        <img src="/files/{{ provider.avatar }}" size="24px" />
        <strong>{{ provider.name }}</strong>
      </div>
      <a href="">+</a>
    </li>
    {% endfor %}
  </ul>
  <a href="/app/logout" class="logout">Sair</a>
</div>
{% endblock %}
